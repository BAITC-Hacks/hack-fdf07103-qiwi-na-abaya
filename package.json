{
  "name": "qiwi-na-abaya",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "next typegen && tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "test": "node --test tests/*.test.ts",
    "check": "npm run typecheck && npm run lint && npm test && npm run build",
    "db:generate": "prisma generate",
    "predb:migrate": "node scripts/init-db.ts",
    "db:migrate": "prisma migrate dev",
    "predb:deploy": "node scripts/init-db.ts",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "prisma db seed",
    "db:setup": "npm run db:generate && npm run db:deploy && npm run db:seed",
    "db:verify": "node scripts/verify-db.ts",
    "db:studio": "prisma studio",
    "db:rescore": "node scripts/rescore.ts"
  },
  "dependencies": {
    "@prisma/client": "6.19.3",
    "lucide-react": "^0.577.0",
    "next": "16.3.6",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "dotenv": "^17.2.0",
    "eslint": "^9.39.0",
    "eslint-config-next": "16.3.6",
    "prisma": "6.19.3",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "overrides": {
    "deepmerge-ts": "^8.0.0"
  },
  "type": "module"
}
