# Relative to prisma/schema.prisma. Keep this file on persistent local storage.
DATABASE_URL="file:./dev.db"

# Server-only. Empty key enables the complete deterministic fallback.
AI_API_KEY=
AI_MODEL=gpt-4o-mini
